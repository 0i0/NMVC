!!! 5
html.no-js(lang='en')
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible",content="IE=edge,chrome=1")
    
    block title
    
    meta(name="description",content="")
    meta(name="viewport",  content="width=device-width,initial-scale=1")
    
    link(rel="stylesheet", href="css/style.css")
    block styles
    
    block topScripts

    - if (DEBUG)
      script(src="/libs/modernizr/modernizr.js")
    - else
      script(src="http://cdnjs.cloudflare.com/ajax/libs/modernizr/2.0.6/modernizr.min.js")

  body
    block content
    //scripts start
    script(type="text/javascript",src="/socket.io/socket.io.js")
    //- script(type="text/javascript")
    //-   var socket = io.connect('!{config.orign}');
    
    block bottomScripts
    //scripts end
    - if (!DEBUG)
      // GA Start
      script(type="text/javascript")
        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', '!{config.google.analyticsAccount}']);
        _gaq.push(['_setDomainName', window.location.host]);
        _gaq.push(['_trackPageview',location.pathname + location.search  + location.hash]);
        (function() {
          var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
          ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();
      // GA End